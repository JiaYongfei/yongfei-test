name: manual

on:
  workflow_dispatch:
    inputs:
      commit:
        description: 'Commit'
        required: false
        default: ''
  repository_dispatch:
    types: [release]
  push:
    tags:
      - 'v*.*.*'


env:
  VERSION: ${{ github.sha }}

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - if: ${{ github.event.inputs.commit != '' }}
      name: Set env
      run: echo "VERSION=${{ github.event.inputs.commit }}" >> $GITHUB_ENV
    - if: ${{github.event_name == 'push'}}
      name: Set env
      run: echo "VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
    - uses: actions/checkout@v2
      with:
        ref: ${{ env.VERSION }}
    - name: Build
      run: |
        echo "Hello event name: ${{github.event_name}}!"
        echo "Hello event name: ${{github.event.action}}!"
        echo "Hello input: ${{ github.event.inputs.commit }}!"
        echo "Hello env version: ${{ env.VERSION }}!"
        echo "Hello github sha: ${{ github.sha }}!"
  #      ./test.sh
  #      go test -v ./test
  #      echo "Done"
  
  release1:
    runs-on: ubuntu-latest
    needs: release
    steps:
    - name: Test
      run: echo ${{ env.VERSION }}
