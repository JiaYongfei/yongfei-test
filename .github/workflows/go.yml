name: Go

on:
  push:
    branches: [ JiaYongfei-patch-5 ]
  pull_request:
    branches: [ JiaYongfei-patch-5 ]

env:
  key: join((${{github.event.head_commit.message}},${{github.event.pull_request.title}},${{github.event.pull_request.body}}),";")

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo $key
    - name: Test4
      if: contains(key, 'one') || contains(key, 'two')
      run: echo 'success'
